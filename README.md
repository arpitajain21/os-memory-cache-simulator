# os-memory-cache-simulator
A C++–based Operating Systems simulator implementing dynamic physical memory allocation (First Fit, Best Fit, Worst Fit), fragmentation analysis, and a multilevel CPU cache hierarchy (L1/L2) with FIFO replacement. Includes a CLI-driven interface, detailed memory visualization, and cache hit/miss statistics.
